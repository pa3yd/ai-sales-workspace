# AI 询盘 Agent · 第六轮优化报告（左侧队列 → Sales Work Queue）

**结论先行**：左侧队列已完成从"历史询盘列表"到"销售工作队列"的升级。
**Agent 核心零改动，数据库核心表结构零改动，全部为展示层最小化修改。**
最终回归：`test_sidebar_r6.py` **32/32 通过**，`test_sidebar_ui.py` **24/24 通过**。

---

### 1. 修改了哪些文件

| 文件 | 改动性质 |
|---|---|
| `workbench/db.py` | 只在 `list_inquiries` 的 SELECT 末尾追加 `i.customer_id`（旧调用方解包兼容，不破坏任何现有字段顺序） |
| `workbench/queue_ui.py` | 新增 3 个无 Streamlit 依赖的纯函数：`wait()` / `customer_key()` / `group_customers()` |
| `workbench/app.py` | 侧边栏重写（搜索/筛选/排序/统计行/空状态/独立滚动/聚合组/整卡点击），Tab1 新增上一条/下一条导航 |
| `test_sidebar_ui.py` | 断言按第六轮新行为更新（6 个排序选项、空状态新文案、聚合折叠组展开后再查 #ID） |
| `test_sidebar_r6.py` | **新增**，32 个测试覆盖第六轮全部验收点 |

### 2. 修改了哪些组件

仅 UI 展示层：
- **卡片渲染**：新 `_queue_card_html()` / `_render_queue_card()`（状态点、公司名、意图·数量、国旗·国家·时间·等级、等待时长、整卡透明覆盖按钮）
- **客户聚合**：组头卡片（N 个询盘 · M 个待回复）+ 点击展开/收起（`_toggle_group`）
- **筛选联动**：4 张 KPI 卡片和"AI 今日建议"点击后自动设置筛选（`_set_filter` on_click 回调）
- **导航**：`_open_inquiry`（整卡点击打开）、`_nav_queue`（上一条/下一条，沿当前筛选+排序的 `queue_ids` 顺序）
- **空状态**：搜索无结果（含"清除搜索"按钮）、筛选无结果、库中无询盘三种
- **CSS**：`.sq` 卡片样式、聚合组 `.ghead/.child`、透明覆盖按钮、侧边栏 292px、独立滚动容器

### 3. 新队列能力（第六轮）

1. **客户视觉聚合**：同客户 ≥2 条询盘折叠为一个组（显示"N 个询盘 · M 个待回复"），点组头展开；选中项所在组自动展开
2. **整卡可点击**：移除旧"打开 → #N"按钮，点卡片任意位置即打开该询盘
3. **等待处理时长**：卡片显示"刚刚 / 32分钟 / 5小时 / 2天 / 7天+"，基于 `created_at`，不虚构数据
4. **AI 今日建议 → 筛选联动**：点建议卡自动切到对应筛选
5. **KPI → 筛选联动**：点"待回复/待补信息/高优先级"KPI 卡自动筛选
6. **空状态**：三种场景各有明确提示与出路（清除搜索按钮）
7. **独立滚动**：队列区 `st.container(height=)` 固定高度内部滚动，不挤占主区
8. **上一条/下一条**：Tab1 顶部按钮，严格跟随当前筛选+排序顺序，只切换查看对象不触发重新分析
9. **默认排序"AI优先级最高"**：按 `calc_priority()` 分数排序

### 4. 搜索 / 筛选 / 排序实现

- **搜索**（`inbox_search`）：对 公司名/联系人/产品/询盘ID 做大小写不敏感子串匹配，附"清除搜索"按钮（on_click 回调清空，规避 Streamlit 实例化后赋值限制）
- **筛选**（`segmented_control`）：全部 / 待回复 / 待补信息 / 高优先级 / 跟进中 五档
- **排序**（selectbox，6 项）：AI优先级最高（默认）/ 待处理优先 / 最新询盘 / 最久未回复 / 报价准备度 / 高商机分
- **选中保护**：筛选/排序后若当前选中项不在结果中，自动选中首个结果（基于 `queue_ids`）

### 5. 客户聚合实现

- 身份键 `customer_key()` 优先级：**customer_id → 规范化公司名（trim+lower）→ 联系人 → 询盘自身 id**（兜底，绝不误合并）
- `group_customers()` 保持首次出现顺序输出 `(key, [items])`
- **无模糊匹配、无 AI 合并、不删除不改任何询盘记录**，纯展示层聚合；`customer_id` 直接复用 DB 已有的外键（仅在 SELECT 中补查）

### 6. 是否修改数据库

**未修改核心表结构**。唯一改动是 `list_inquiries` 查询 SELECT 末尾追加 `i.customer_id` 字段（读操作），未增删表、未改列、未动任何写入逻辑。旧代码按索引解包的调用方不受影响（新字段追加在末尾）。

### 7. 是否修改 Agent

**Agent：未修改。**
extractor / matcher / lead_score / gapcheck / insight / reply_strategy / replier 全部原样，prompt 未动，评分逻辑未动，报价准备度未动，产品匹配未动，回复草稿未动，跟进逻辑未动，客户信息提取未动，数量识别未动，认证逻辑未动，防跨询盘污染逻辑未动。

### 8. 完成了哪些测试

| 套件 | 结果 | 覆盖 |
|---|---|---|
| `test_sidebar_r6.py`（新增） | **32/32 ✅** | wait() 边界、customer_key() 身份键、group_customers() 顺序；验收 Test A 聚合、B/C 筛选、D/E 搜索+空态+清除、F AI优先级排序（按分数而非等级名）、G 整卡点击、H AI建议联动、L 上一条/下一条、KPI 联动、queue_ids 写入 |
| `test_sidebar_ui.py`（回归） | **24/24 ✅** | 相对时间/国旗单元、渲染无异常、统计行、搜索 placeholder、6 排序项、#ID 区分、整卡点击、旧按钮删除、数量未识别、搜索命中、空状态、ID 搜索、选中高亮、点击跟随、同客户展开后各自带 #ID |

### 9. 已知问题（不阻塞使用）

1. **多询盘客户默认折叠**：组内子卡片展开后才可见（设计如此）；选中项所在组会自动展开，但通过搜索定位到折叠组内的询盘时组头会显示、子卡片需点开
2. **等待时长基于 created_at**：尚无"首次查看时间"字段，无法区分"挂着没看"和"看了没回复"（属 DB 层增强，本轮未做）
3. **透明覆盖按钮依赖 CSS `:has()`**：需较新浏览器（Chrome 105+），旧浏览器退化为点不到卡片（按钮仍在原位可见性为 0 时不影响其他按钮）
4. AppTest 无法覆盖真实浏览器交互（hover、实际滚动），已用 session_state 与渲染断言替代

### 10. 下一轮优先级建议

1. **"已读/未读"状态**：加首次查看时间戳，等待时长才能反映真实跟进滞后
2. **聚合组内排序**：展开后子卡片按等待时长或优先级排序
3. **键盘快捷键**：J/K 或 ←/→ 直接切换上一条/下一条
4. **筛选条件持久化**：刷新后记住上次的筛选+排序组合
5. **组级批量操作**：对一组询盘批量标记已处理/生成跟进草稿

---

⚠️ **提醒**：如果 Streamlit 工作台正在运行，请重启（Ctrl+C 后重新 `streamlit run app.py`）以加载第六轮新代码。
