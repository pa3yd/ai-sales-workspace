# AI 外贸询盘工作台 · 第七轮优化报告（销售执行闭环）

**结论先行**：工作台已从"AI 询盘分析工具"升级为"AI 外贸销售工作台"——
每条询盘都有【当前状态 / 为什么 / 卡在哪 / 下一步做什么 / 点了会发生什么】，
回复 → 发送 → 跟进 → 报价 → 成交/丢单 全链路打通。
**Agent 分析核心、反幻觉规则、产品匹配、评分算法：零改动。**

最终测试：`test_workflow_r7.py` **65/65** ✅ · 回归 `test_sidebar_r6.py` **32/32** ✅ ·
`test_sidebar_ui.py` **24/24** ✅ · `test_offline.py`（Agent 核心）全部通过 ✅

---

### 1. 修改了哪些文件

| 文件 | 改动 |
|---|---|
| `workbench/workflow.py` | **新增**：12 态业务状态机、AI Finding→Next Action 映射、跟进状态计算（纯函数，无 Streamlit 依赖） |
| `workbench/db.py` | 兼容迁移 5 个新列 + activity 表 + 7 个闭环函数（详见第 5 节） |
| `workbench/app.py` | 详情页 CTA/发送/跟进/结单/时间线、左侧卡片 Next Action、筛选/排序/AI建议扩展 |
| `test_workflow_r7.py` | **新增**：65 个测试 |

### 2. 修改了哪些功能

- **详情页头区**：状态徽章升级为 12 态业务状态（已回复/待跟进/已报价/已成交…）
- **Primary CTA**（spec 十九，唯一主动作）：按 Next Action 自动映射——补信息类→[生成询问]、发送类→[查看并发送]、报价类→[准备报价]、跟进类→[跟进]、终态→禁用按钮显示"等待客户反馈"
- **标记为已发送**（spec 七/八）：REPLIED + last_replied_at + 事件记录，**只改 CRM 状态，不发真实邮件、不伪造发送**
- **跟进工作流**（spec 八/九）：24小时/48小时/3天/7天/自定义 五档设置跟进时间；状态 自动计算：待跟进/今日跟进/已逾期/已完成；到期后进入"今日需跟进"待办
- **结单**（spec 二十二/二十三）：WON/LOST 必须人工勾选确认；AI 永不自动标记成交
- **Activity 时间线**（spec 十七）：真实业务事件持久化记录（ANALYZED→REPLY_GENERATED→REPLIED→FOLLOW_UP→WON…），旧记录无事件时保留派生兜底（不伪造历史）
- **左侧队列**（spec 十一）：每张卡片和聚合组头新增一行"下一步：确认产品"式短句，不显示大段 AI 分析
- **AI 今日建议**（spec 十/二十六）：新增"⏰ N 条询盘今日需要跟进 → [立即跟进]"，点击进入「待跟进」筛选——全部动态计算，KPI/建议/筛选/详情同一数据源
- **筛选/排序**（spec 二十五）：筛选新增「待跟进」；排序新增「今日待办优先」（今日需人工处理 > 高优先级 > 待回复 > 需设跟进 > 缺信息 > 普通 > 已完成），默认仍为 AI优先级最高（不破坏现有行为）

### 3. 新增了哪些状态

**12 态业务状态**（`workflow.py`）：NEW / ANALYZING / NEEDS_INFO / READY_TO_REPLY / REPLIED / FOLLOW_UP / READY_FOR_QUOTE / QUOTED / NEGOTIATING / WON / LOST / ON_HOLD。
**兼容原则**：旧记录 biz_status 为空 → 从现有两态+阻塞+报价准备度派生（已处理→待跟进、有阻塞→待补信息、报价就绪→可报价、否则→待回复）；两态 待处理/已处理 完整保留，所有旧逻辑不受影响。
**AI判断 / 业务状态 / 报价准备度 三概念严格分开**：B级是评分、READY_FOR_QUOTE 是业务状态、92分是准备度，互不混淆。

### 4. 新增了哪些 Action

统一 Action 结构 `{type, label, priority}`，映射（spec 六）：
CONFIRM_PRODUCT 确认产品 · CONFIRM_QUANTITY 确认数量 · CONFIRM_SPECIFICATION 确认规格/定制/交期/包装 · CHECK_CERTIFICATION 确认认证 · CHECK_SAMPLE 确认样品 · SEND_CATALOG 发送目录 · REVIEW_REPLY 查看并发送 · CREATE_FOLLOW_UP 设置跟进时间 · FOLLOW_UP_CUSTOMER 立即跟进 · CREATE_QUOTE 生成报价 · FOLLOW_UP_QUOTE 跟进报价反馈。
**13 个 Action 类型全部结构化**（不依赖 AI 自然语言建议），阻塞项 P0 恒定最优先。

### 5. 数据流如何变化

- `inquiries` 表新增 5 列（ALTER TABLE 兼容迁移，不动任何现有列）：`biz_status / last_replied_at / follow_up_at / follow_up_done / deal_status`
- 新表 `activity(inquiry_id, type, ts, description, actor)`——严格按 inquiry_id 绑定
- 新函数：`mark_replied / set_follow_up / complete_follow_up / set_deal / record_activity / list_activity / get_workflow`
- `list_inquiries` 末尾追加 wf 字段字典（旧调用方 row[:11] 解包不受影响）
- **业务动作驱动状态**（spec 二十）：点[标记为已发送]自动 REPLIED→提示设跟进；设跟进自动排期待办；到期自动变"立即跟进"；结单需人工确认。用户无需手动改状态

### 6. 状态机如何变化

- 明确合法转换表（`workflow.can_transition`）：如 NEW→WON **禁止**、NEEDS_INFO→QUOTED **禁止**、QUOTED→WON 允许、LOST→NEGOTIATING 允许（死单复活）、WON 为终态
- 结单 UI 只在状态机允许时出现（待回复阶段没有结单入口，测试覆盖）
- 数据库层不硬拦（防旧数据卡死），UI 层按状态机隐藏非法入口

### 7. 哪些旧逻辑保持不变

**Agent：未修改**——extractor / matcher / lead_score / gapcheck / insight / reply_strategy / replier、反幻觉规则、报价准备度算法、评分算法、防跨询盘污染逻辑全部原样。
数据库两态（待处理/已处理）、优先级算法 calc_priority、第六轮全部 UI（客户聚合/整卡点击/独立滚动/上一条下一条/空状态）原样。回复草稿仍按 `draft_box_{id}` 按询盘隔离，Reply Draft 与 Analysis 的一致性校验（draft_issues 警示）原样保留。

### 8. 完成了哪些测试（spec 二十八 十场景全覆盖）

| 场景 | 结果 |
|---|---|
| 01 产品未知+数量明确 | ✅ NEEDS_INFO / 确认产品 |
| 02 信息完整 | ✅ READY_FOR_QUOTE / 生成报价（有草稿则查看并发送） |
| 03 数量冲突/缺失 | ✅ 确认数量，不自行选定数量 |
| 04 客户要目录 | ✅ SEND_CATALOG，不强制先答一堆问题 |
| 05 客户要认证 | ✅ 仅阻塞项时 CHECK_CERTIFICATION(P0) |
| 06 回复生成 | ✅ READY_TO_REPLY / 查看并发送 |
| 07 标记已发送 | ✅ REPLIED + 事件 + 下一步设跟进 |
| 08 48小时后 | ✅ 已逾期 → 立即跟进 → 进入"今日需跟进"建议 |
| 09 客户回复后重析 | ✅ REPLIED→ANALYZING→NEEDS_INFO/READY_FOR_QUOTE 合法 |
| 10 跨询盘隔离 | ✅ B 页面无 A 的产品/数量/草稿；草稿框按 #id 隔离 |
| 业务闭环（二十九） | ✅ ①→⑧ 全链路：NEEDS_INFO→READY_TO_REPLY→REPLIED→跟进到期→报价就绪→QUOTED→WON，事件序列连续（ANALYZED…WON） |
| 状态机防护 | ✅ NEW→WON 禁止；待回复无结单入口；成交仅人工 |

### 9. 测试结果

`test_workflow_r7.py` **65/65** · `test_sidebar_r6.py` **32/32** · `test_sidebar_ui.py` **24/24** · `test_offline.py`（Agent 核心）**全部通过**。测试使用临时数据库，未触碰真实数据。

### 10. 当前还存在什么问题

1. **"生成报价"没有独立报价模块**：当前 CTA [准备报价] 打开回复草稿区（附产品库价格参考），不会虚构价格/MOQ/交期；正式报价单生成留待下轮
2. **"已处理"旧记录语义模糊**：历史上点过"标记跟进"的记录派生为"待跟进"，但没有 last_replied_at；UI 不显示发送时间（不伪造）
3. **跟进时间修改后旧事件不撤销**：改期会追加新的 FOLLOW_UP_CREATED 事件（时间线如实记录，不删历史）
4. **REPLIED→WON 允许直接成交**：业务上有"没报价直接下单"的情形，故状态机放行，但必须人工勾选确认
5. 跟进到期检测在页面加载时计算（无后台定时任务），需要打开工作台才会看到"今日需跟进"

### 11. 下一轮最值得优化的一个方向

**报价模块（Quote）**：把"报价准备度=READY_FOR_QUOTE"的询盘接上真正的报价单生成——从产品库取真实价格区间/MOQ/交期，人工确认后生成正式报价 PDF/邮件，状态自动推进 QUOTED，并记录 QUOTE_CREATED 事件。这是闭环里目前唯一还缺的实体单据，也是"成交"前最关键的一步。

---

⚠️ **提醒**：请重启 Streamlit 工作台（Ctrl+C → `streamlit run app.py`）加载第七轮代码（旧进程也有自愈式 reload 兜底，但建议彻底重启）。
