# 产品架构规划报告 · AI 外贸销售工作台（第二阶段：从"工具"到"工作台"）

> 基线：第一阶段项目审计（已完成）｜日期：2026-09-07｜性质：**只读设计，未改任何代码**
> 覆盖：产品信息架构 / 页面结构 / 页面关系 / 数据模型关系 / 用户核心操作路径 / 推荐开发优先级
> 配套图：① 目标信息架构图 ② 目标数据模型关系图（对话内嵌）

---

## 0. 产品定位与设计原则

**一句话定位**：打开系统 = 进入"今天该处理谁、为什么、下一步做什么"的销售工作流；**每个页面都是工作流的一个环节，而不是一堆可浏览的数据表**。首页尤其不是 Dashboard。

沿用你的核心产品原则（每页/每卡回答五问）：

| # | 问题 | 载体 |
|---|---|---|
| 1 | 销售现在该做什么？ | 卡片上的动作按钮 / 待办桶 |
| 2 | 为什么现在做（AI 判断依据）？ | "AI 依据"一行字（事实源 / 规则名） |
| 3 | 当前客户/商机在什么阶段？ | 状态 chip（可手动修正） |
| 4 | 下一步可执行动作？ | 动作区：回复 / 报价 / 跟进 / 改期 |
| 5 | 完成后状态如何变、系统进入什么？ | 自动写活动 + 推进状态 + 进入下一条 |

**增量设计原则**（由审计第 0 节引伸，延续你的工作原则）：
1. 对象能**派生就不建表**（如 Opportunity 现阶段保持只读派生）；
2. 必须建表时，新表加 `R1x_XXX` 版本标记 + 幂等自愈迁移（沿用现模式），并对旧字段**双写兼容**；
3. AI 全部保留**离线规则兜底**（双引擎），LLM 只是增强；
4. 每次只落一个模块，可独立回归，旧断言不删、新口径先写成测试。

---

## 1. 产品信息架构（总览）

```
AI 外贸销售工作台
│
├── 执行层（每天泡在这里——回答"现在做什么"）
│    ① 今日工作台（默认首页 · 销售 Inbox）   ← 不是数据面板
│    ⑥ 跟进中心（防漏：今日到期 / 已逾期 / 改期）
│
├── 对象层（查与管——回答"客户/询盘/产品在哪"）
│    ② 询盘中心    ③ 客户中心    ④ 产品中心    ⑤ 报价管理
│
├── 洞察层（复盘——回答"销售推进健康吗"）
│    ⑦ 数据分析（漏斗 / 逾期 / 维度下钻）
│
└── 系统层（运行——回答"系统归谁管"）
     ⑧ 系统设置（API / SELLER / 卡密 / 备份）
```

**导航演进建议**：由现在的「侧栏销售队列 + 5 Tab」升级为「**左侧全局导航 + 页面内对象列表**」；原侧栏队列迁入①今日工作台成为其左栏，避免"侧栏队列"与"首页队列"两套概念并存。过渡期（M1）保留原 Tab 结构、仅新增首页，最后再切路由。

**旧→新页面归位**：

| 现在（审计） | 归入目标页 |
|---|---|
| Tab 分析询盘（含 render_report 详情） | → ①今日工作台（新建分析入口）+ ②询盘中心（详情/行动队列） |
| Tab 跟进台 + 侧栏销售队列 | → ①今日工作台（队列/桶）+ ⑥跟进中心（跟进清单） |
| Tab 客户档案 | → ③客户中心 |
| Tab 产品库 | → ④产品中心 |
| Tab 导出 + KPI + 今日行动摘要 | → ⑦数据分析（报表）+ ⑧系统设置（备份/导出） |

---

## 2. 页面结构设计（8 页）

### 2.1 ① 今日工作台（默认首页 · Inbox）

**布局**：左＝待办桶；右＝当前任务处理台（选中任意卡片/桶后进入）。

**八桶口径**（每桶＝预置筛选＋排序＋数量角标，不是静态报表）：

| 桶 | 口径（数据来源） | 排除条件 |
|---|---|---|
| 今日到期跟进 | follow_ups.due_at = 今天 且未完成（新表；兼容 inquiries.follow_up_at） | 已 done |
| 已逾期跟进 | due_at < 今天 且未完成 | 已 done |
| 高优先级询盘 | priority pts ≥ 65 或紧急 | 已回复/已关闭 |
| 待回复 | 已分析但未回复（replied_at 为空） | 已回复 |
| 报价机会 | quote_readiness = READY 且无有效报价单 | 已有 SENT 有效报价 |
| 高价值客户 | 评级 A/B 且商机开放或近 30 天活跃 | — |
| 停滞商机 | 商机开放 且 最后活动/跟进 > 7 天 | WON/LOST |
| AI 推荐动作 | 上表之外的 workflow.next_action 汇总 | 无动作 |

**任务卡信息**：客户 / 公司 / 国家 / 产品需求 / 数量 / 阶段 chip / 优先级 / 最后跟进时间 / **AI 下一步建议（1 行动作＋1 行依据）**。

**右屏任务台（自动步进）**：当前客户 → 当前询盘 → 销售阶段（可改，走统一入口）→ AI 下一步建议 → **执行动作**（回复 / 生成报价 / 记录跟进 / 改期 / 补询盘信息）→ 登记结果（写 activity + 生成下一条跟进）→ **"完成并进入下一条高优任务"**。

**兜底态**：桶全空时给出动作引导（去新建分析 / 去跟进 / 去建报价），不放空图表。

### 2.2 ② 询盘中心

**列表列**（按你要求的 12 项映射）：AI 优先级、客户、公司、国家、产品需求（extracted 摘要）、数量（quantity 语义）、当前阶段（biz_status）、当前状态（已回复/跟进中/成交等）、下一步动作（next_action）、最后跟进时间（follow_up/activity）、AI 建议（reply_plan 摘要）、草稿状态（是否已生成）。

**列表操作**：搜索、筛选（阶段/国家/产品/优先级/时间区间）、排序（优先级/最近/金额潜力）、批量（标记已回复/改期/删除）。

**详情**：保留现有 render_report（评分、缺口、事实层、草稿、HUMAN REVIEW 横幅）不动；顶部加"销售推进条"（阶段＋建议＋动作）；底部保留行动队列入口。

**新建分析**：粘贴原文入口（Tab 级或弹层），分析完成后跳同一详情模板。

### 2.3 ③ 客户中心

**结构**：公司 → 联系人 →（该公司）询盘 / 商机 / 报价 / 跟进 / 成交历史。

**公司列表卡**：名称 / 国家 / 评级 A–D / 开放商机数 / 最近互动 / 高价值标记（AI 画像摘要一行）。

**公司详情＝纵向切页**：

| 子页 | 内容 |
|---|---|
| 概览 | 客户 360 画像（历史询盘聚合/采购模式/价格敏感度/活跃度，规则＋LLM 摘要）+ 基本信息 + owner（预留） |
| 联系人 | 联系人列表（现 contacts JSON 兼容；完整拆表留到多用户阶段） |
| 询盘 | 该公司全部询盘（时间线列表） |
| 商机 | 由询盘派生的商机及状态（M2 前只读） |
| 报价 | 该公司报价单（M2 起） |
| 跟进 | 跟进记录（M1 起） |
| 活动时间线 | 现有 activity 事件流（扩展类型） |
| 备注/标签 | 手动备注与标签 |

### 2.4 ④ 产品中心

**层级**：分类 → 类型 → 型号 → 规格/SKU（对齐 insight 现有产品层级语义）。

**产品字段集**：产品名称 / SKU / 产品分类 / 规格 / MOQ / 价格（含阶梯价）/ 交期 / 产品状态 / 产品标签 / 产品资料（附件或链接）/ HS 编码 / 库存与安全库存（保留现有）。

**产品状态**：在售 / 开发中 / 停产 / 下架。

**列表**：分类树过滤 + 搜索 + 标签筛选；**详情**：基本信息 + 价格阶梯表 + 资料 + 低库存提示（保留现有 stock 逻辑）。

**迁移**：M3 由 products.json 迁 SQLite（提供导入器保持 JSON 兼容），期间 matcher/insight/报价先读新表。

### 2.5 ⑤ 报价管理

**对象＝报价单（header + lines）**——这是工作台区别于"询盘分析器"的最实质缺口（审计 P2）。

- **header**：报价单号 / 客户公司 / 联系人 / 关联询盘 / 关联商机 / 报价日期 / 有效期 / 币种 / 贸易条款（FOB/CIF…）/ 总金额 / 状态 / 版本 / 备注。
- **lines**：产品（名称+SKU 快照）/ 数量 / 单价 / 金额 / MOQ 校验 / 交期快照。
- **状态机**：`DRAFT → SENT → ACCEPTED（→商机 WON）/ REJECTED（→商机 LOST）/ EXPIRED`，版本升级 +1。
- **生成入口**：询盘详情/商机卡在 quote_readiness=READY 且无有效报价时出现"生成报价单"，预填客户/询盘/产品需求/价格阶梯；也可在报价管理页新建。
- **动作**：保存草稿 / 标记已发送 / 登记客户回复（AI 分析语气） / 复制全文 / 导出 .eml（.pdf 留到 M5）。
- **金额口径**：报价单金额以 lines 汇总为准，与 crm.opportunity_of 的"目标价×数量"粗估解耦。

### 2.6 ⑥ 跟进中心

**对象化（M1 建 follow_ups 表）**——从单字段升级为一等实体（审计 P3）。每条跟进＝：

| 维度 | 取值 |
|---|---|
| 关联对象 | 公司 / 询盘 / 商机 / 报价（多态） |
| 渠道 | 邮件 / WhatsApp / 电话 / 报价发出 / 客户回复 / AI 建议 / 其他 |
| 内容 | 摘要＋结果 |
| AI | 建议快照（下次跟进时机/话术） |
| 时间 | 计划 due_at → 完成 done_at → 下次 next_due_at |

**视图**：今日到期 | 已逾期 | 未来计划 | 已完成（历史）。支持**批量改期 / 批量完成**；行点击跳对象详情。

**完成流程**：登记结果 → 自动追加 activity →（AI 建议 next_due 与话术，可一键采纳）→ 从"今日"桶移除。

**兼容**：新表写入的同时**双写 inquiries.follow_up_at**，保证旧页面与既有测试口径不破。

### 2.7 ⑦ 数据分析

- **KPI 区**：新增询盘 / 待回复 / 待跟进 / 已报价 / 成交（周环比）。
- **漏斗**（activity 事件口径聚合）：`ANALYZED → REPLIED → FOLLOWED_UP → QUOTED → WON`；附逾期榜 / 平均首响时长 / 平均成交周期。
- **维度下钻**：产品 / 国家 / 客户分级（A–D 转化差异）/ 来源（需补 platform 字段，M5 可选）。
- 输出"销售健康度"一句话摘要（规则聚合，非 LLM）。

### 2.8 ⑧ 系统设置

API 与模型（key 由 config.json **迁环境变量**）｜SELLER 公司资料真实化（认证/付款/质保/交期）｜卡密授权（接入 licensing，含到期提示）｜数据备份/一键还原/导出｜关于/版本。

---

## 3. 页面之间的关系

**全局导航**：8 页互跳；对象间通过 ID 深链互跳（列表行 → 详情 → 子对象）。

**关键跳转**：

| 从 | 动作 | 到 |
|---|---|---|
| 今日工作台 任意桶/任务卡 | 点击 | 询盘详情 / 客户详情 / 报价单 |
| 询盘详情 商机卡 | readiness=READY | 新建报价单（预填客户/询盘/产品） |
| 客户详情 商机/报价卡 | 点击 | 商机推进 / 报价单 |
| 报价单 | SENT/ACCEPTED/REJECTED | 回写商机 WON/LOST ＋ 写时间线 → 首页桶即时变化 |
| 跟进中心 行/完成 | 点击/完成 | 对象详情 / 写 activity → 今日桶减少 |
| 数据分析 | 下钻 | 各对象列表 |

**状态回写规则**：任何"完成类"动作收敛到 **db 层单函数**（唯一状态变更入口），修复审计 P2 的双口径分叉风险。

---

## 4. 数据模型关系（目标）

配套对话内嵌②数据模型图。文字版：

```
Company(公司) 1—N Contact(联系人)
Company(公司) 1—N Inquiry(询盘)；Contact 1—N Inquiry
Inquiry —N:1→ Opportunity(商机，M2 前纯函数派生 crm.opportunity_of)
Opportunity 1—N Quotation(报价单)
Quotation 1—N QuotationLine(报价行) N—1 Product(产品快照，防改价)
FollowUp(跟进) —(多态)→ Company / Inquiry / Opportunity / Quotation
Activity(活动) = 全对象事件总线（时间线 + 漏斗数据源）
```

**对象字典（现 → 目标）**：

| 对象 | 现状（审计） | 目标关键字段 | 落点 | 新表 |
|---|---|---|---|---|
| Company | customers 表（ckey 归并） | +国家/行业/网站/owner(预留)/最近互动 | 小改 | 否 |
| Contact | customers.contacts JSON | 主联系人字段结构化；完整拆表→多用户阶段 | 兼容 | 否 |
| Inquiry | inquiries + report_json | 现有列已覆盖 12 项展示；统计用 platform/来源 → M5 补列 | 小改 | 否 |
| Opportunity | crm.py 派生 | 报价闭环后物化：金额/预计成交/阶段历史 | M2 | 可选 |
| Quotation | 无 | header+lines（见 2.5） | M2 | 是×2 |
| Product | products.json | 层级/SKU/阶梯价/标签/认证/状态/资料 | M3 | 迁表(保导入) |
| FollowUp | 无（单字段 follow_up_at） | 多态对象+渠道+AI快照+due/next | M1 | 是 |
| Activity | 有 | 事件类型扩展（QUOTE_SENT/ACCEPTED/FUP_CREATED…） | 小改 | 否 |
| License | licensing 独立 | 接入 CLI+工作台+到期 | M5 | 否 |

**迁移护栏**：新表全部带 `R1x_XXX` 版本标记＋自愈 reload；新增列一律幂等 ALTER；双写窗口内旧逻辑零改动可回归；每个新口径先固化为单元测试（沿用 109 套"新规范覆盖旧断言"做法）。

---

## 5. AI 演进路线（分析工具 → 销售智能体）

| AI 能力 | 现状（文件） | 升级目标 | 载体页面 | 阶段 |
|---|---|---|---|---|
| Lead Qualification | lead_score（规则+LLM） | 客户级**连续评分**（跨询盘聚合）与趋势 | 询盘中心/客户概览 | M1/M3 |
| Customer Analysis | 无（手工 grade/note） | 客户 360 画像：采购模式/价格敏感度/活跃度 | 客户概览＋高价值桶 | M3 |
| Product Matching | matcher | 消费产品层级与标签，输出匹配理由 | 询盘详情 | M3 |
| Quote Readiness | insight 四态 + gapcheck | 与报价单联动：READY → 一键生成报价 | 询盘/商机卡 | M2 |
| Next Best Action | workflow.next_action | 升级为**八桶待办聚合器**（统一口径，首页即结果） | 今日工作台 | M1 |
| Reply Generation | replier + Fact Guard | 语气档/多语言；发出后自动登记跟进 | 询盘详情回复区 | M2 |
| Follow-up Assistant | 无（今日行动摘要＝规则） | 到期扫描 + AI 建议下次跟进时机/话术 + 上次跟进摘要 | 跟进中心＋今日桶 | M1（M2 增强） |

**底线**：每条 AI 能力保留规则兜底；任何 AI 判断必须显示"依据"（事实层 Source 或规则名），延续 Fact Guard 精神。

---

## 6. 用户核心操作路径（4 条）

**Path A · 处理新询盘**
今日工作台（或询盘中心）"新建分析" → 粘贴原文 → 分析（评分/缺口/事实层/草稿）→ 按 AI 建议补齐缺口 → 回复（复制全文/发出）→ 登记跟进（自动写 activity）→ 完成 → 自动下一条

**Path B · 推进存量客户**
今日工作台挑高优/待跟进 → 客户详情看商机 → AI 建议 → 执行动作（回复/报价/跟进/改期）→ 记录 → 状态自动更新 → 自动跳下一条

**Path C · 报价闭环**
商机＝具备报价条件 → 一键生成报价单（预填）→ 审核改价 → 标记已发送 → 客户回复登记（AI 分析）→ 接受＝商机 WON / 拒绝＝记录原因商机 LOST → 时间线留痕 → 首页桶更新

**Path D · 防漏跟进**
跟进中心 今日/逾期 → 逐条完成（登记结果）或批量改期 → AI 建议 next_due → 逾期归零 → 数据页漏斗可见推进

---

## 7. 目标对审计问题的消除表

| 审计问题（P） | 由哪个目标页/对象消除 | 阶段 |
|---|---|---|
| P1 闭环入口弱（无默认收件首页） | ①今日工作台＝默认首页，8 桶即入口 | M1 |
| P2 状态双轨、无唯一变更入口 | 状态变更收敛 db 单函数（3 节） | M1 起 |
| P2 报价不闭环、无报价实体 | ⑤报价管理 + Quotation 对象 | M2 |
| P2 产品与询盘两套存储、无层级 | ④产品中心 + Product 迁库层级化 | M3 |
| P3 跟进单字段非实体 | ⑥跟进中心 + follow_ups 表 | M1 |
| P3 无漏斗/转化报表 | ⑦数据分析 | M4 |
| P1 明文 Key / SELLER 占位 / 卡密未接线 | ⑧系统设置 + licensing 接线 | M5 |
| P3 app.py 单文件膨胀 | 每新增页收进 workbench/views/ 独立模块 | 各阶段 |

---

## 8. 推荐开发优先级（里程碑）

| 里程碑 | 内容 | 验收锚点 | 主要文件 | 回归 |
|---|---|---|---|---|
| **M1** 今日工作台 + 跟进实体化 | follow_ups 表（R2x 标记）；workflow 八桶聚合纯函数；默认首页＝今日工作台（原队列迁入）；跟进中心页（逾期/批量改期） | 打开即见 8 桶；完成动作自动下一条；逾期批量改期；八桶口径单测 | db.py / workflow.py / app.py（或 views/inbox.py、views/followups.py） | 既有全套 + test_r12_inbox + 真实浏览器 E2E |
| **M2** 报价对象闭环 + 回复落地 | quotations + lines 表；生成器复用 replier/价格；状态机回写商机；复制全文/.eml | READY→一键报价→SENT→ACCEPTED 转 WON 全链路 | db.py / 新 quote 模块 / app.py（报价区）/ crm.py 金额口径 | 既有全套 + test_r13_quote |
| **M3** 产品层级化 + 客户/AI 增强 | products 迁 SQLite（保 JSON 导入）；层级/标签/阶梯价；matcher/insight/报价消费；客户 360 画像 | 新结构被 matcher/报价消费；产品详情展示阶梯价 | products 迁移脚本 / catalog.py / matcher.py / insight.py / app.py | 既有全套 + 迁移往返测试 |
| **M4** 数据分析页 | activity 事件补齐；漏斗/逾期榜/维度下钻 | 漏斗五段可算；下钻跳对象列表 | db.py（聚合查询）/ app.py（analytics） | 既有全套 |
| **M5** 分发就绪 | key 迁环境变量；SELLER 真实化；licensing 接线；备份/一键还原；打包说明 | 新电脑装好验卡可用；无明文密钥 | llm_client.py / main.py / app.py / licensing / .bat | 全套 + 全链路 E2E |

**依赖**：M1 无外部依赖，可独立验证；M2 依赖 M1（报价后登记跟进）；M3 可与 M2 并行但都在 app.py 加页 → 串行执行更稳；M4 依赖 M2 事件；M5 收尾。
**多用户（M6）＝门控项**：需先做产品决策（是否多人共用、数据归属模型），默认不进入本路线，M5 前冻结产品边界。

---

## 9. 兼容与风险护栏

1. **存量库兼容**：一切 schema 演进沿用 `R1x_XXX` 版本标记＋幂等 ALTER＋自愈 reload（历史 12 vs 11 事故的教训）。
2. **双口径**：Opportunity 派生 vs 物化之间设"以物化为主、派生兜底"的过渡开关；follow_up 双写旧字段直到旧 UI 下线。
3. **app.py 膨胀**：每个里程碑把新增页面收进 `workbench/views/` 独立模块，render_report 不再加长。
4. **LLM 依赖**：同步调用需 loading 态与超时提示（现状 UI 会卡住无反馈）。
5. **密钥外泄**：M5 完成前**不要对外分发**项目包。
6. **测试盲区**：AppTest 会绕过 CSS 几何，任何 UI 改动必须补真实浏览器 E2E 抽查（沿用已验证通道）。

---

**产品架构规划完成，等待下一步指令。**（下一步可选：确认后从 M1"今日工作台＋跟进实体化"开始出详细设计与编码。）
