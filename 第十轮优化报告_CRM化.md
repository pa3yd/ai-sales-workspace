# 【第十轮优化报告】AI 外贸询盘助手 → AI 外贸销售 CRM Workspace

> 执行路径：审查报告（A~F）→ 你确认 → 8 个 Phase 增量实施，每步跑回归。
> 说明：指令标题为"第九轮"，与已完成的第九轮（业务事实层）重名，本轮按**第十轮**执行。

---

## 1. 修改了什么

| Phase | 修改内容 |
|---|---|
| 1 左侧队列 CRM 化 | 客户组卡升级为**公司视角五段布局**：① 状态点+公司名+商机阶段徽章 ② 询盘数 · 待回复数 · 商机金额 ③ 最新需求摘要 ④ **客户级下一步**（跨询盘挑 P0>P1>P2）⑤ 国家 · 最近活动 · AI 级。Inquiry ID 弱化为子卡小字 |
| 2 客户/联系人关系 | 客户档案新增**多联系人层**（同公司不同联系人自动累计，只追加不覆盖）；tab3 升级为**客户视角页**：客户概览 + 联系人列表 + 当前商机块 + 最近沟通 Top5 |
| 3 商机 Opportunity | **轻量派生，不建新表**：一个客户一个商机——阶段从该客户全部询盘的业务状态推进度派生；商机金额 = Σ(数量×目标价)，缺任何数据就跳过（绝不编造） |
| 4 Next Action | 详情页行动卡升级为**五要素**：动作 / 优先级(P0-P2) / 截止 / 负责人 Sales / 状态。所有 AI 分析最终落到行动 |
| 5 AI 行动队列 | 点击"AI 今日建议"不再只切筛选，而是进入**连续处理模式**：1/N 进度 + AI 判断（等级/分数/原因）+ 下一步 + [生成回复][标记完成][稍后处理][查看客户] + 上一个/下一个 + 退出队列 |
| 6 Activity Timeline | ANALYZED / REPLY_GENERATED 事件**下沉到入库点统一记录**（每条询盘必有时间线起点）；时间线自动补派生事件（已标记发送/计划跟进），不伪造历史时间 |
| 7 主工作区层级 | 详情页顶部新增 **7 段商机状态条**（✓已过 ●当前 ○未来，丢单/暂缓特殊标注）——3 秒知道客户走到哪一步；评分依据保持默认折叠（原本已折叠，确认符合 spec 十二） |
| 8 搜索/筛选/排序 | 搜索新增**联系人、产品**维度；新增"高级筛选"折叠区（报价准备度 / 商机阶段 / 国家）；排序新增"最久未跟进" |

## 2. 新增了什么

- **`workbench/crm.py`**（新模块，纯函数可单测）：商机九态（新商机/需求确认/产品匹配/待报价/已报价/谈判中/已成交/已丢单/长期培育）、客户级商机派生 `opportunity_of()`、7 段漏斗定位 `funnel_position()`。
- **两个新测试**：`test_crm_r10.py`（44 项）、`test_scenarios_r10.py`（验收场景 37 项）。
- 审查报告文档 `第十轮审查报告_CRM化.md`。

## 3. 被修改的代码文件（6 个）

`workbench/crm.py`（新增）、`workbench/db.py`、`workbench/app.py`、`test_crm_r10.py`（新增）、`test_scenarios_r10.py`（新增）、`test_crm_r8.py`（1 处断言随新语义更新，已注明原因）。

## 4. 没有修改的业务逻辑

- Agent 四模块（extractor / matcher / lead_score / replier）核心逻辑 **零改动**
- 所有 Prompt **零改动**
- 12 态业务状态机及其合法转换规则 **零改动**（商机阶段只是它的派生视图）
- 事实层 / 防串数据机制（第九轮全部成果）**零改动**
- 现有筛选 chip 文案、排序默认行为、产品库、导出 **零改动**

## 5. 数据结构是否变化

**只追加，无破坏性变更**：
- `customers` 表新增 `contacts` JSON 列（ALTER 兼容旧库）
- `need` 派生字典新增 product / qty_num / price_num / target_price_currency / has_draft（只读展示派生）
- activity 表新增 ANALYZED / REPLY_GENERATED 两种事件类型（表结构不变）
- 没有新表，没有删除/改名任何字段，旧数据全部兼容（biz_status 为 NULL 时照旧派生）

## 6. 测试结果（全绿）

| 套件 | 结果 |
|---|---|
| test_crm_r10（本轮商机单元） | **44/44** ✅ |
| test_scenarios_r10（验收 Test 01~05） | **37/37** ✅ |
| test_sidebar_r6 / test_crm_r8 / test_sidebar_ui | 32/32 · 32/32 · 24/24 ✅ |
| test_workflow_r7（状态机+闭环） | 65/65 ✅ |
| test_patch03 / test_quantity / test_reply_consistency / test_r5p3 / test_r5 | 32 · 47 · 11 · 39 · 109 全过 ✅ |
| test_r9_facts（第九轮事实层） | 41/41 ✅ |
| test_offline + AppTest 冒烟 | 通过 ✅ |

**验收场景覆盖（spec 二十二 10 项检查）**：5 个场景全部验证了 Company/Contact 识别、Inquiry 关联、Opportunity 形成、Next Action、Status、Timeline、历史数据隔离、快速处理路径与现有 AI 能力保留。

## 7. 发现的问题（本轮抓到并修复）

1. **存量 bug：队列"有草稿→查看并发送"从未触发**——`_row_has_draft` 读的是 row[10]（实际是 urgency 而非 report_json）。修复：db 派生 `need["has_draft"]`。
2. **事件记录点分散**：ANALYZED 只在页面流程记录，测试/外部写入不会有时间线起点。已下沉到 `db.save_inquiry` 入库点统一记录。
3. 一个待观察点：商机金额是"数量×目标价"的粗估，UI 已标注 ≈ 号；切勿在对外报价场景把它当报价（第九轮事实层的"客户目标价 ≠ 公司报价"原则继续生效）。

## 8. 下一轮建议

1. **报价单生成**：READY_FOR_QUOTE 状态 → 基于产品库价格生成正式报价单（配合公司数据 company_profile.json），并把 QUOTED 状态与报价记录回写——这是当前漏斗里"待报价→已报价"唯一还靠人工的环节。
2. **Opportunity 实体化（可选）**：如果将来需要"同一客户多条产品线多个商机"，再把派生视图升级为独立 opportunity 表；当前单商机派生视图对外贸场景够用，不必提前复杂化。
3. **行动队列直达跟进**：行动队列里的"标记完成"目前是转"已处理"；可按 Next Action 类型智能分流（确认产品→打开追问邮件，跟进→打开跟进设置）。
4. **客户画像 AI 化**：客户视角页的"AI 画像"目前是评级+统计；可加一句 LLM 生成的客户画像摘要（历史询盘聚合，注意走 allowed_facts 防串数据）。
5. **提醒自动化**：到期跟进目前要打开系统才能看到；可用定时任务每天 8 点把"今日待办"推送到对话。
