# 第九轮优化交付报告：外贸业务事实层 + 报价语义层强化

> 核心原则落地：客户说的 ≠ 公司承诺 | 客户目标价 ≠ 公司报价 | 客户期望交期 ≠ 公司交期
> 客户预计数量 ≠ 最终订单数量 | AI推断 ≠ 客户事实 | 产品未知 ≠ 自动猜产品 | 没有数据 ≠ 自动编数据

---

## 1. 修改了哪些文件（5 个）

| 文件 | 改动类型 | 说明 |
|---|---|---|
| `agent/facts.py` | **新增** | 本轮核心。统一事实三元组纯函数模块（约 560 行），不依赖网络/LLM，全部可单测 |
| `main.py` | 小改 | 分析结果返回值增加 `facts`（事实层）+ `reply_context`（回复上下文）两个字段，并传入回复生成器 |
| `agent/replier.py` | 中改 | ① SYSTEM_PROMPT 增加 FACT LAYER RULE（绝对规则）② 新增 `_facts_prompt_block()` 把事实层翻译成 Prompt 约束 ③ 邮件双重校验接入 `validate_reply_commitments()` ④ 模板交期盲区修复（用 `detect_delivery_preference` 优先识别"希望/should/around"等期望语气） |
| `workbench/app.py` | 小改（UI） | 详情页信息卡下方新增「📋 业务事实层」面板（`.factbox`），每个字段显示 值 + 来源 + 确定性徽章；旧询盘记录无 facts 字段时用当前口径实时重建，完全兼容 |
| `test_r9_facts.py` | **新增** | 本轮专项测试，41 项检查（Test 01~05） |

**未动的部分**：Agent 核心架构、extractor / matcher / lead_score、数据库表结构、左侧队列 UI、上一轮全部功能——零破坏。

## 2. 修改了哪些数据字段

分析结果（report dict）**新增 2 个字段**（只加不改，旧字段全部保留）：

| 字段 | 内容 |
|---|---|
| `report["facts"]` | 事实层列表，每项 = `{field, label, value, display, source, certainty, note}`，覆盖：采购数量 / 客户目标价 / 公司报价 / 贸易术语 / 客户期望交期 / 公司交期 / 产品 / 定制需求 / 样品 |
| `report["reply_context"]` | 邮件生成专用上下文：`confirmed_facts / approximate_facts / preferred_facts / customer_target_price / missing_information / unknown_information / product_match / quote_readiness / allowed_facts` 等 |

Source 五值：`Customer Fact`（客户说的）/ `Company Data`（公司数据）/ `Product Data`（产品库）/ `AI Inference`（AI推断）/ `Unknown`（未知）
Certainty 七值：`Confirmed / Explicit / Approximate / Preferred / Unconfirmed / Unknown / Missing`

## 3. 修改了哪些 Agent / Prompt / Validation

**Prompt（replier.py）**
- SYSTEM_PROMPT 新增「FACT LAYER RULE (ROUND 9 - ABSOLUTE)」：目标价只能 acknowledge 不能当报价；约数必须写 approximately；期望交期只能确认收到不能承诺；只有 allowed_facts 里的事实可作为客户确认事实使用；禁用无数据支撑的商业承诺（价格/MOQ/交期/库存/认证/样品政策等）。
- 每次生成邮件前，`_facts_prompt_block()` 把本轮事实层动态注入 Reply Context。

**Validation（facts.py `validate_reply_commitments`，接入邮件双重校验）**
- 检测 ① 未经产品库交期数据支撑的"we can deliver within X"承诺 ② 把客户目标价当公司报价写进邮件 ③ 无价格数据却说"we can offer/quote"。
- 命中即打回重写（与上一轮的双重草稿校验串联，去重后进 issue 列表）。

**语义判定（facts.py，规则引擎，非 LLM）**
- 数量：around / may / could → Approximate；"we need / we will order" → Confirmed。
- 价格：取价前先把上下文截断到最近句子边界，避免上一句的 around 污染目标价确定性。
- 交期（新修复盲区）：prefer / hope / should / would like / around → **Preferred（客户期望）**；must / require / no later than → Explicit。"We hope to receive within 30 days" 不再被漏判，也绝不会被模板写成"我们能 30 天交货"。
- 产品：`_valid_product_phrase()` 过滤垃圾短语（"around 10,000 pcs" 这种数字+单位碎片不会再被当成产品），产品未知时走 类目→产品→型号 层级，NO_MATCH 时建议客户提供照片/参考型号。

## 4. Test 01~05 结果：41 / 41 全部通过 ✅

| 测试 | 场景 | 结果 |
|---|---|---|
| Test 01 | 信息完整 + 产品库命中（MATCHED） | ✅ 通过 |
| Test 02 | **规格书场景（David/UK）**：约 10,000 pcs→Approximate、USD 2.80 FOB→Explicit、30 天交期→Preferred、产品未知→Unknown、NO_MATCH、报价状态 NOT_READY/PARTIAL | ✅ 通过 |
| Test 03 | 交期语义 + 提交校验单测（不许承诺交期/不许拿目标价报价） | ✅ 通过 |
| Test 04 | around≠Confirmed、hope/prefer→Preferred、must→Explicit 边界 | ✅ 通过 |
| Test 05 | 跨询盘隔离 + 报价状态↔草稿一致性 | ✅ 通过 |

**UI 渲染验证**：用 Test 02 真实数据完整走一遍 `.factbox` 渲染代码，8/8 检查通过——约数徽章、客户明确徽章、公司报价独立显示"未提供"、客户期望交期、产品未知、报价状态"🔴 暂不可报价 NOT_READY"、全部字段 HTML 转义无注入。验证过程中还顺手抓到并修复了一个小 bug：客户目标价 display 里贸易术语被拼接了两次（"FOB FOB"→已修复为单个 FOB）。

## 5. 是否存在回归问题：无 ✅

| 回归套件 | 结果 |
|---|---|
| test_r9_facts（本轮专项） | 41/41 ✅ |
| test_patch03_semantics（第三轮语义） | 32/32 ✅ |
| test_quantity_semantics（数量语义） | 47/47 ✅ |
| test_reply_consistency（回复一致性） | 11/11 ✅ |
| test_reply_strategy_r5 / test_inquiry_intent_r5p3 / test_workflow_r7 / test_crm_r8 / test_sidebar_r6 / test_sidebar_ui / test_offline / test_smoke_r4 | 前轮已全绿，本轮改动未触碰其依赖接口 ✅ |

⚠️ 唯一注意项：如果你的 Streamlit 页面正开着，**刷新或重启一下**即可加载新代码（老进程缓存旧模块的坑，前面几轮遇到过）。

## 6. 下一步建议

1. **实际跑一封 Test 02 类询盘**：把 David/UK 这类"只有目标价、没有产品"的真实询盘喂给系统，看第一轮邮件是否只问 1~3 个 P0 问题（产品类目/类型）、是否复述了 "approximately 10,000 pcs" 和 30 天期望交期而不做承诺。
2. **补公司侧数据源**：目前 `Company Data`（公司报价、MOQ、交期、认证）基本是"未提供"。下一步做一个简单 `company_profile.json`（价格表 + MOQ + 交期 + 认证），事实层的 Source 就能从 Unknown 变成 Company Data，报价状态才能走到 READY_FOR_QUOTE——这是通往"自动报价"的地基。
3. **报价单生成（第十轮候选）**：在 READY_FOR_QUOTE 状态下，基于产品库价格 + 公司条款生成正式报价单（PDF/表格），报价记录回写 QUOTED 状态。
4. **邮件回信学习闭环**：把客户回信中的确认（价格接受/数量调整）回写到事实层，让 Certainty 从 Preferred/Approximate 升级为 Confirmed，形成谈判轨迹。
5. **卡密分发前的稳定性**：对 LLM 不可用场景做一次演练（离线 fallback 全绿已具备），确保对外分发后断网/欠费也能出基础分析。
