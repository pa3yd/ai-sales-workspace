# 第十一轮优化报告：CRM 销售工作台（信息层级 + 交互）

> 说明：用户指令标题为"第十轮"，因上一轮已完成"第十轮（CRM 化）"，本轮按序执行为**第十一轮**。
> 范围：只动 UI / 信息层级 / 交互；**Agent 核心逻辑、Prompt、数据结构零改动**。

---

## 1. 修改了哪些 UI 组件

| 组件 | 改动 | 对应规格 |
|---|---|---|
| 顶部 KPI 卡（今日新增/高优先级/待处理/AI 建议行动） | 高度、字号、圆角压缩（.kpi CSS），从主视觉降为一行轻量指标 | §三 |
| AI 今日建议 | 改为「🤖 今日行动摘要」紧凑条，标题 + 每行小字 + 小按钮，点击仍直达对应工作队列 | §三/八 |
| 新建询盘分析区 | 190px 大输入框收进 `＋ 新建询盘分析` 折叠区，**默认收起**；顶栏按钮自动展开；分析完成自动收起并进入详情 | §十 |
| 商机状态条下方 | 新增「当前阶段：XX（AI 建议）」+ **阶段手动修改下拉**（受状态机约束，防跳变；写入 STATUS_CHANGE 时间线事件） | §五 |
| Next Action 行动卡 | 新增「原因：」行——AI 不只说做什么，还说为什么（来自 next_actions reason / 阻塞项派生） | §七 |
| 队列导航条 | 改为 `← 上一个 │ N/M · 当前队列：XX │ 下一个 →`，新增**「✅ 完成并进入下一条」**（标记完成 + 自动切下一条） | §九 |
| 回复策略区 | 「目标」改为「**回复目的**」；新增「**❓ 建议问题**」块——逐条列出首轮问题（≤3，与草稿同源 reply_plan.selected） | §十四 |
| 详情页信息层级 | Next Action 卡 + 按钮行整体上移到「待处理/缺失信息」之前：客户+状态 → 阶段 → AI 判断 → **下一步行动** → 缺失信息 → 指标带 → AI 分析 → 回复 → 跟进 | §十三 |

## 2. 修改了哪些交互

- 点「＋ 新建询盘分析」（顶栏）→ 展开粘贴区并清空旧内容，粘贴 → 分析 → 直接送入详情页，粘贴框自动收起。
- 阶段下拉改动 → 状态机校验（非法跳变被拒绝并提示）→ 落库 → 时间线记录"销售阶段变更"。
- 「完成并进入下一条」→ 当前询盘标记已处理 + 自动跳到队列下一条，不回列表。
- KPI 卡 / 今日行动摘要按钮 → 应用对应筛选并进入 AI 行动队列（沿用第十轮能力）。

## 3. 新增了哪些 CRM 能力

- **阶段人工可改**（AI 建议为默认值）：销售可按真实进展推进/回退阶段，状态机防跳变，全部进时间线。
- **完成并推进**：批量处理队列时零返回列表。
- **建议问题显式化**：首轮 1-3 个问题在回复区一目了然（此前只在草稿正文里）。
- **AI 建议带原因**：行动卡三行结构 = 做什么 + 为什么 + 五要素（优先级/截止/负责人/状态）。

## 4. 是否修改 Agent 逻辑

**未修改。** 询盘解析、产品匹配、评分、缺失信息、报价准备度、回复生成、防幻觉、事实层全部原样。本轮只消费已有输出字段（reply_plan.selected、next_actions.reason、quotation_readiness）。

## 5. 是否修改数据结构

**未修改。** 阶段手改复用第八轮已有的 `inquiries.biz_status` 列；时间线复用 activity 表，仅新增一种事件类型 STATUS_CHANGE（枚举值，非结构变更）。

## 6. 测试结果

**新增验收 test_scenarios_r11.py：34/34 通过**（离线规则引擎口径）：

- Test 01 产品未知+3000pcs：P0 问题=确认参考型号 ✅ 绝不显示可正式报价 ✅
- Test 02 水瓶 500ml/750ml+3000pcs：产品不在库 → NO_MATCH 且追问参考型号（**不编造产品**）✅ 数量 3000 正确 ✅ 草稿无未依据承诺 ✅
- Test 03 多数量（trial 500 / initial 3000 / potential 10000）：语义分层 ✅ 主数量取首单 ✅ 不把 10000 当确认数量 ✅
- Test 04 认证：识别为 interest ✅ **不绝对阻塞报价**（可初步报价）✅
- Test 05 Catalog-only：不编造 ✅ 停留在需求确认 ✅ 不可正式报价 ✅
- Test 06 目标价 USD 2.80：客户目标价单独成项 ✅ 公司报价=未提供 ✅ 草稿不拿 2.80 当我方报价 ✅
- Test 07 期望交期 4 周：识别为客户期望（Preferred）✅ 草稿未承诺交期 ✅ 防幻觉校验无违规 ✅
- Test 08 EuroSource 综合：数量分层 ✅ 防编造 ✅ 问题≤3 ✅ 定制意图识别 ✅ 目标价/公司报价分离 ✅ 阶段↔准备度一致 ✅
- 状态机：NEEDS_INFO→QUOTED 禁止 ✅ REPLIED→QUOTED 允许 ✅ LOST→NEGOTIATING（死单复活）允许 ✅

**回归：12 个历史套件全部绿，共 516 项检查**（r9 41 · r10 44 · r5 109 · r7 65 · patch03 32 · 数量 47 · 侧边栏 32+24 · r8 32 · 意图 39 · 一致性 11）。
AppTest 无头冒烟 5/5：页面无异常、详情页四要素、折叠入口、完成并进入下一条、阶段下拉。

## 7. 当前仍存在的问题（均为引擎既有行为，本轮未改）

1. **数量角色标注偶有误标**："initial order 2000-3000 pcs" 在含 "annual" 词语的上下文里被标为 ANNUAL_VOLUME（试单 500 识别正确）。数量提取主值不受影响，仅 role 标签偶错。
2. **认证强度判定偏保守**："with CE and RoHS certification" 判为 interest（兴趣），"We need CE..." 才判 required（强制）。偏保守不会造成过度承诺，属安全方向，但与部分真实语料的语气强度有差距。
3. **产品库只有 6 个游泳用品**：水瓶等未入库产品一律 NO_MATCH + 追问参考型号（这是正确的防编造行为，但意味着 Test 02 场景的实际价值是"防编造验证"而非"匹配验证"）。
4. 跟进区仅在「已回复/待跟进/已报价/谈判/可报价」状态下显示——需求确认阶段无法设置跟进提醒（低优先级）。

## 8. 下一轮最值得优化的 3 个方向

1. **报价单生成**：补上「可报价 → 已报价」的人工环节——读取产品库价格 + 已确认数量/条款，生成结构化报价单（PDF/表格），落库并记录 QUOTE 事件。这是目前销售流程中唯一还靠线下的环节。
2. **数量角色标注修正 + 认证语气分级**：针对第 7 节问题 1/2 做小步修正（收紧 ANNUAL_VOLUME 的上下文窗口；certification 增加 "required-soft/required-hard" 分级），各配回归用例。
3. **邮件接入/发送前确认流**：回复草稿 → 人工确认 → 记录"已发送"（先做 SMTP 或剪贴板流转均可），把「标记为已发送」升级为真实发送凭证，Timeline 完整闭环。

---
**验证方式**：`python test_scenarios_r11.py`（34 项）+ 12 个历史回归套件。刷新 Streamlit 页面即可看到全部改动。
