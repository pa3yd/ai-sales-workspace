# 第八轮优化报告：询盘列表 → CRM 商机队列

本轮定位：不是重设计页面、不是重写 Agent，而是把左侧列表的**数据逻辑与信息层级**升级为
「客户 → 商机 → 询盘 → 当前阶段 → 下一步动作」。坚持最小改动：未新增任何数据表、未改 Agent。

---

### 1. 修改了哪些文件

| 文件 | 改动 |
|---|---|
| `workbench/app.py` | 统计行加客户数；筛选 chip 语义统一；排序 tie-breaker；组头升级为商机卡；详情页 cmeta 加「商机：」 |
| `test_crm_r8.py` | **新增**，32 条断言（10 项验收 + 身份键单元 + 空状态） |
| `test_sidebar_r6.py` / `test_sidebar_ui.py` | 3 处断言跟随 chip 改名更新（「待补信息→待补关键信息」「跟进中→待跟进」） |

未修改：`workflow.py`、`queue_ui.py`、`db.py`、`agent/*`（全部复用第六/七轮成果）。

### 2. 修改了哪些组件

- 左侧队列统计行（+客户数）
- 左侧筛选 segmented_control（chip 重命名 + 过滤口径改为商机阶段）
- 左侧排序（AI优先级最高加次级 tie-breaker）
- 客户聚合组头卡（升级为完整商机卡）
- 详情页头区 cmeta（+当前商机）
- AI 今日建议 / KPI 点击筛选的落点值（与新 chip 对齐）

### 3. 增加了哪些数据字段

**零新增持久化字段。** 全部复用现有：customer_id、biz_status（第七轮）、
report 里的 blockers/readiness/intent/quantity、优先分 pts。纯展示层重组。

### 4. 是否增加了 Customer 聚合逻辑

聚合逻辑第六轮已存在，本轮**复用并验证**：customer_id > 规范化公司名 > 联系人 > 询盘自身 id，
无模糊合并。新增 6 条身份键单元测试锁死行为（含"Beta Trade Co ≠ Beta Trade Co."）。

### 5. 是否增加 Opportunity Stage

未新建数据结构，复用第七轮 workflow 12 态作为商机阶段口径，并完成**语义统一**：
筛选 chip、统计行、卡片徽章、组头阶段，全部使用 `workflow.BIZ_CN` 同一套词表
（待回复 / 待补关键信息 / 已回复 / 待跟进 / 已报价 / 已成交…），
消灭了旧版「跟进中/待补信息」与阶段名不一致的问题。

### 6. 是否增加 Next Action

第七轮已建立（AI Finding → 结构化 Action 映射），本轮把它提升为客户卡核心信息：
组头第二视觉层级显示「下一步：xxx」，与阶段、询盘数并列（spec 十的视觉层级）。

### 7. 是否修改 API

无 API 层。Streamlit 内部函数签名未变。

### 8. 是否修改数据库

**未修改。** 无新表、无新列、无迁移。

### 9. 是否修改 Agent

**Agent：未修改。** prompt、评分、匹配、反幻觉、报价准备度、缺失信息判定、跨询盘隔离全部原样
（`test_offline.py` 离线回归通过佐证）。

### 10. 十项测试结果（test_crm_r8.py，32/32 通过）

| # | 验收项 | 结果 |
|---|---|---|
| 01 | 同客户 8 询盘 → 仅一张组头卡 | ✅ |
| 02 | Beta Trade Co ≠ Beta Trade Co. 不误合并 | ✅ |
| 03 | 8 个询盘 · 5 个待回复（阶段口径计数） | ✅ |
| 04 | 最新询盘待回复 → 阶段「待回复」+ 下一步「回复客户」 | ✅ |
| 05 | 产品缺失 → 下一步「确认产品」（非空泛 AI 建议） | ✅ |
| 06 | 已报价 → 阶段「已报价」+ 下一步跟进类 | ✅ |
| 07 | 展开历史询盘 + 点击加载对应询盘、无跨询盘污染 | ✅ |
| 08 | 刷新后 selected_id / 阶段 / 下一步保持 | ✅ |
| 09 | 删除一条后组头 8→7 实时更新 | ✅ |
| 10 | 无 id/邮箱/公司 → 各自回落自身 id，不强归并 | ✅ |

另有身份键单元 6 条 + 空状态 1 条。回归：R7 65/65、R6 32/32、UI 24/24、Agent 离线全过。

### 11. 已知问题

- 组头「待回复」计数已统一为阶段口径（READY_TO_REPLY），旧版按两态计数的语义不变但数字可能变小（缺信息的归入「待补关键信息」，更准确）
- 客户数按当前筛选范围统计，「全部」下为客户总数；无独立客户去重持久化（展示层计算）
- 商机仍是从询盘聚合出的视图（spec 二允许的轻量方案），无独立 Opportunity 表——数据量大或需要商机级备注/合并时才值得建表

### 12. 下一轮最建议优化的一个方向

**跟进台（Tab2）与商机队列打通**：目前「待跟进」商机已能被识别和筛选，但跟进台的批量操作、
跟进记录补录还没有与组头/商机视图联动。打通后「每天打开→看待办→批量跟进」才算真正闭环。
