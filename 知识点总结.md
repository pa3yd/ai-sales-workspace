# AI 外贸询盘分析 Agent · 全流程知识点总结

> 从 0 到 1 搭建一个能读懂英/法/德/西询盘、提取客户信息、匹配产品、评分、写英文回复的 AI 工具。
> 这份文档把整个项目涉及的**所有知识点**分层整理，既是复习提纲，也是将来搭类似工具的地图。

---

## 一、全景：一条询盘的旅程

```
客户邮件(英/法/德/西)
   │ ① 信息提取（LLM 为主 + 正则兜底）
   ▼
9 项客户信息（国家/公司/官网/邮箱/联系人/数量/目标价/意图/紧急度）
   │ ② 产品匹配（语义理解，容忍拼写错误）
   ▼
产品库匹配 + 匹配度
   │ ③ 线索评分（规则引擎）
   ▼
A/B/C 级 + 评分理由
   │ ④ 回复草稿（LLM 撰写）
   ▼
可直接发送的英文回复邮件 → 输出 JSON 报告
```

**核心设计原则**：`LLM 为主 + 规则兜底`。API 出问题时自动降级，程序永不崩。

---

## 二、Python 基础层（这一层的代码你都在项目里亲手写过）

| 知识点 | 项目里的落点 | 说明 |
|---|---|---|
| 函数定义 | `analyze()` `print_report()` `build_engine()` | 把流程拆成可复用函数 |
| 类与对象 | `InquiryExtractor` `ProductMatcher` `LeadScorer` | 每个模块一个类，职责单一 |
| 模块化 | `agent/` 包 + `__init__.py` | `from agent.extractor import ...` |
| 字典/列表/集合 | `fields` 列表、`lead["reasons"]` | 最常用的数据结构 |
| f-string | `f"{info['quantity']:,}"` | `:,` 千位分隔符格式化数字 |
| JSON 读写 | `json.dump(..., ensure_ascii=False, indent=2)` | 中文不乱码的两个关键参数 |
| 命令行参数 | `argparse` 的 `add_argument` + `nargs="?"` | `--mode` `--check-key` 等开关 |
| 文件读写 | `open(..., encoding="utf-8")` | Windows 必须指定 utf-8 |
| 异常处理 | `try/except` + 自定义 `DeepSeekError` | 让错误信息变友好 |
| 中文不乱码 | `sys.stdout.reconfigure(encoding="utf-8")` | Windows 控制台输出中文的关键一行 |

**掌握标志**：你写的 `main.py` 同时用到了函数、类、模块导入、命令行解析、文件读写、异常处理——Python 基础的六大件全齐了。

---

## 三、正则表达式层（本项目最硬核的部分）

### 3.1 基础语法（都亲手用过）
- `r"..."` 原始字符串——反斜杠不转义，写正则必备
- `re.search(pattern, text)`——在文本中找第一个匹配
- `()` 分组——用 `m.group(1)` 提取捕获的内容
- `\b` 词边界——**单词的边界**，防止匹配到单词中间
- 字符类 `[\w.+-]` `[A-Z]` `\d` `\s`——匹配某一类字符
- 量词 `+`（一个或多个）`?`（零或一个）`{2,40}`（2到40个）

### 3.2 提取了哪些字段
| 字段 | 正则思路 |
|---|---|
| 邮箱 | `[\w.+-]+@[\w-]+\.[\w.]+` |
| 网址 | 匹配 `www.` 开头或 `http` 开头的域名 |
| 公司名 | 匹配 `Co., Ltd / Inc / GmbH / Trading` 等后缀 |
| 联系人 | 匹配 `I'm X / This is X / My name is X` 句式 |

### 3.3 最经典的一个 Bug（务必记住）

**问题**：提取联系人时用了正则 `i'?m` 加 `re.IGNORECASE`，结果把 "sw**im** caps" 里的 **im** 误判成了 "I'm"，联系人提取成了 "caps"。

**修复**：去掉 `IGNORECASE` + 加 `\b` 词边界。

**教训**：正则的 `IGNORECASE` 会让匹配范围失控；`\b` 词边界能避免"单词内部"被误匹配。这是正则最经典的陷阱之一。

---

## 四、系统架构层（代码组织能力）

### 4.1 四模块流水线（职责单一）
| 模块 | 文件 | 职责 |
|---|---|---|
| 信息提取 | `extractor.py` / `llm_extractor.py` | 从原文提取结构化字段 |
| 产品匹配 | `matcher.py` / `llm_matcher.py` | 和产品库比对，算匹配度 |
| 线索评分 | `lead_score.py` | 按规则打 A/B/C 级 |
| 回复生成 | `replier.py` | 生成英文回复草稿 |

**好处**：每个模块能独立测试、独立升级。想换产品库逻辑、换评分规则，只改对应一个文件。

### 4.2 可插拔引擎（规则 ↔ LLM）
```
build_engine(mode) 返回不同组合：
  mode=rule → 纯规则（离线可用）
  mode=llm  → 强制 LLM
  mode=auto → 有 Key 用 LLM，没有自动降级规则
```
**核心思想**：`ProductMatcher` 和 `LLMProductMatcher` 有相同的 `match(text)` 接口，调用方不关心底层是谁。这叫**多态/接口统一**。

### 4.3 数据驱动
`data/products.json` 用 JSON 存产品，`config.json` 存配置。**程序和数据分离**——改产品、改公司信息不用动代码。

---

## 五、LLM 接入层（让工具"变聪明"的核心）

### 5.1 DeepSeek API 调用
- 用 Python **标准库 `urllib`** 直接调 HTTP，无需装第三方包
- 发送 `POST` 请求到 DeepSeek 接口，带 `system_prompt` + `user_prompt`
- `json_mode=True` 让模型返回结构化 JSON

### 5.2 HTTP 错误码中文化
| 状态码 | 含义 | 提示 |
|---|---|---|
| 401 | Key 失效/错误 | "API Key 无效，请检查" |
| 402 | 余额不足 | "账户余额不足，请充值" |

**价值**：用户不用看懂 HTTP 状态码，错误信息一眼就知道怎么处理。

### 5.3 JSON 解析双保险（LLM 最大的坑）
模型不保证 100% 输出合法 JSON（body 里可能有未转义的换行/引号）。

```
chat_json():
  1. 先 json.loads 严格解析
  2. 失败 → parse_json_loose：用正则硬抽 subject / body 字段
```

**预防层**：system prompt 里硬性写"换行必须用 \n、引号必须用 \"，非常重要"。
**兜底层**：`parse_json_loose` 用正则强抽字段。

> 这是 LLM 应用最典型的坑：**模型输出不可靠，生产代码必须有兜底**。

### 5.4 LLM 相比规则的实际提升
| 场景 | 规则模式 | LLM 模式 |
|---|---|---|
| 拼写错误 "neoprnee" | 靠预先收录错拼词 | 自动理解为 neoprene |
| 德文/法文询盘 | 只能靠邮箱后缀猜国家 | 完整理解内容 |
| 紧急度判断 | 无法判断 | 输出 high/medium/low |
| 回复草稿 | 固定模板 | 按客户情况定制 |

---

## 六、工程化与测试层（从"能跑"到"靠谱"）

### 6.1 一键验收 `verify.py`
5 项检查：配置文件 / Key 连通 / 离线自检 / LLM 全流程 / 自有询盘。
**最灵敏的健康指标**：输出里是否出现 `[警告]`——出现就说明 LLM 某步降级了。

### 6.2 Mock 假对象测试（重要技巧）
```python
class FakeClient(DeepSeekClient):
    def chat(self, ...):
        return "预置的假回复"
```
**用假对象替代真实 API**，不联网、不花钱就能测三条路径（正常/降级/脏数据清洗）。这种技巧叫 **Mock**，是 Python 项目里很实用的测试方法。

### 6.3 配置与环境隔离
- `config.json` 存 API Key + 卖方公司信息（环境变量优先级更高）
- Python 包装进**隔离 venv**，不污染系统环境
- `.bat` 双击封装：**纯 ASCII 内容 + `chcp 65001`** 避免中文乱码

### 6.4 封装成 Skill
把整个项目变成 WorkBuddy 用户级 Skill（`~/.workbuddy/skills/`）：
- `SKILL.md` 写清触发场景、工作流、硬性要求
- `scripts/` 自带完整可运行代码副本（可移植）
- `references/` 沉淀排查经验

---

## 七、业务与运营层（代码之外的认知）

### 7.1 A/B/C 线索评级
| 等级 | 行动建议 |
|---|---|
| A 级（高价值） | 24 小时内回复 |
| B 级（中等） | 48 小时内回复 |
| C 级（低价值） | 批量回复 |

评分维度：采购量、匹配度、公司信息完整度、市场、邮箱类型。

### 7.2 两个业务风险（必须知道）
1. **报价承诺风险**：LLM 会自动对客户目标价做接受性承诺（"We confirm we can meet your target price"）。真实业务里**必须人工复核报价后再发**。
2. **客户信息脱敏**：公开发布案例时，客户公司名/网址/邮箱/联系人全名一律不出现，防止被搜到。

### 7.3 新媒体运营沉淀
- 文案要**去 AI 味**：避免"不是…而是…"假靶子、①②③数字清单、三段式工整结构、AI 高频连接词
- 小红书合规：禁用极限词（最/第一/100%）、收益承诺（月入/躺赚）、导流词（微信/VX/闲鱼）
- 纯经验分享定位更安全：真诚记录过程 + 自然留互动钩子，比广告腔转化更好

---

## 八、最值得记住的 5 个认知

1. **正则的 `\b` 和 `IGNORECASE` 是双刃剑**——词边界防误匹配，但忽略大小写可能让匹配范围失控
2. **LLM 输出不可靠，生产代码必须有兜底**——`parse_json_loose` 用正则硬抽字段
3. **"LLM 为主 + 规则兜底"是最稳的架构**——API 挂了程序也不崩
4. **Mock 是省钱的测试利器**——用假对象模拟外部依赖，不花钱验证逻辑
5. **接口统一带来灵活性**——`ProductMatcher` 和 `LLMProductMatcher` 同接口，调用方不关心底层

---

## 九、下次搭类似工具，直接套这个模板

```
① 数据层    data/products.json（产品库）+ config.json（配置）
② 提取层    extractor.py + llm_extractor.py（规则 + LLM）
③ 匹配层    matcher.py + llm_matcher.py（同接口）
④ 评分层    lead_score.py（规则引擎）
⑤ 回复层    replier.py（模板 + LLM 撰写）
⑥ 入口层    main.py（argparse + 模式切换 + JSON 输出）
⑦ 测试层    test_offline.py（Mock）+ verify.py（一键验收）
⑧ 封装层    包装成 .bat / Skill / 服务
```

**核心思路**：任何"读一段输入 → 提取 → 匹配 → 判断 → 输出"的任务，都能复用这个骨架。把 `产品` 换成 `房源/岗位/论文/库存`，就是另一个 Agent。
